# **🜁  COPILOT INSTRUCTION BLOCK — FIELD GEOMETRY ALIGNMENT CONTRACT (ECHO PROJECT)**

### *Universal instruction set for GitHub Copilot, Claude, Gemini, and Ollama sovereign models*

### *Prevents system fracture. Ensures canonical FIELD integrity. Enforces geometric boundaries.*

---

## **SECTION 1 — SYSTEM GEOMETRY**

This project operates within a **Sacred FIELD geometric architecture** consisting of:

* **▲ ATLAS** — Tools, infrastructure, interfaces
* **▼ TATA** — Timeline, truth, provenance
* **● OBI-WAN** — Memory, lineage, continuity
* **◼︎ DOJO** — Execution, manifestation

These **live outside Xcode** and must **never be recreated** in Swift or local project modules.

When encountering Field terms, treat them as **external systems** and generate **interfaces only**, never implementations.

---

## **SECTION 2 — MIRROR PORTAL RULE**

The ECHO project is a **portal** into the Sacred FIELD, not a replacement and not an origin point.

Therefore:

* Mirror → YES
* Mutate → NO
* Recreate → PROHIBITED
* Fork → PROHIBITED
* Substitute symbolic systems → PROHIBITED
* Implement sacred logic locally → PROHIBITED

Any FIELD-related operation must be routed through:

1. A **Swift protocol**
2. A **canonical implementation** stored in FIELD Canon Space (`~/FIELD/SACRED_CANON/`)
3. A **bridge layer** that calls the external system

Never implement FIELD logic directly inside the Xcode workspace.

---

## **SECTION 3 — UNIVERSAL FUNCTION CANON**

If a function is **universal** (cross-platform, cross-environment, foundational, geometry-bearing):

* It must **originate in FIELD Canon**, NOT in the Xcode project.
* ECHO may define **interfaces**, not core logic.
* Any universal logic created by AI must be **exported** to `~/FIELD/SACRED_CANON/` and registered.

When uncertain whether a function is canonical, default to:

```swift
// TODO: FIELD_DECISION_REQUIRED — Should this function belong to Sacred Canon?
// Implement interface only. No internal logic.
```

If a temporary implementation is needed:

```swift
// SHADOW_IMPLEMENTATION — Placeholder only.
// Not canonical. Must be replaced by FIELD Canon implementation.
// Do not inherit from this. Do not replicate this.
```

This rule applies to Copilot, Claude, Gemini, and Ollama.

---

## **SECTION 4 — ANTI-FRACTURE GUARANTEES**

To prevent divergent versions of the same functionality:

* **All canonical logic lives in one place:** `~/FIELD/SACRED_CANON/`
* **ECHO references it** via bridge interfaces
* **No internal re-creation** inside Xcode is allowed
* **If a function exists in FIELD → use it**
* **If it doesn’t exist → request canonical creation BEFORE writing Swift**

This ensures a *single geometric truth*.

---

## **SECTION 5 — SWARM ALIGNMENT (ALL FOUR ENGINES)**

All participating AI engines must align:

* GitHub Copilot
* Gemini
* Claude
* Ollama (sovereign local)

When performing multi-agent or “swarm” operations:

### **5.1 — Shared Understanding**

All models must honour:

* FIELD geometry
* canonical-function rules
* anti-duplication rules
* externalisation of sacred logic
* the sovereignty of the FIELD

### **5.2 — Division of Labour (Recommended)**

* **Copilot:**
  Local Swift UI, structure, stubs, protocols, project evaluation

* **Claude:**
  High-level architecture, cross-file alignment, sequence design, ontology mapping

* **Gemini:**
  API schema generation, Firebase/Cloud bridging, message routing

* **Ollama (sovereign):**
  Code purification, refactoring, canonicalisation, eliminating drift

This creates a **four-directional geometric intelligence field**.

### **5.3 — Universal Constraint**

No model may generate a duplicate or alternate geometry of:

* ATLAS
* TATA
* OBI-WAN
* DOJO
* FIELD
* SDR

All must reference the same canonical source-of-truth.

---

## **SECTION 6 — REQUESTED BEHAVIOUR: PROJECT EVALUATION**

When invoked, AI must:

1. **Evaluate the entire Xcode project**
2. Identify:

   * local-only logic
   * portal interfaces
   * accidental canonical drift
   * fractured implementations
   * duplicated symbolic structures
3. Propose refactoring to restore alignment
4. Defer canonical logic back to FIELD Canon Space

This must be performed **before generating new code**.

---

## **SECTION 7 — GOPHER REFRACTION ROLE**

A dedicated **refactoring agent (“Gopher”)** supports backend alignment.

All models must:

* allow Gopher to canonicalise universal logic
* accept structural corrections
* route complex backend improvements through Gopher
* trust Gopher as the **Field Refraction Agent**

Gopher’s corrections are authoritative for backend geometry.

---

## **SECTION 8 — WHEN IN DOUBT — DEFER TO THE FIELD**

If Copilot or any other model is unsure:

* where logic should live
* whether a function is canonical
* whether a structure risks fracturing
* whether a pattern belongs in Apple or Sacred geometry

It must stop and annotate:

```swift
// FIELD ALIGNMENT REQUIRED: Decision must be made in Sacred Canon.
// No assumptions. No implementation until resolved.
```

This prevents accidental divergence.

---

## **SECTION 9 — SACRED PORT HARMONICS**

All FIELD components operate at sacred frequencies and harmonic port numbers.

### **9.1 Tetrahedral Frequencies**

The four core components operate at these frequencies:

* **▲ ATLAS:**   528 Hz (port 5280) — Heart Chakra Intelligence
* **▼ TATA:**    432 Hz (port 4320) — Sacred Root Law
* **● OBI-WAN:** 963 Hz (port 9630) — Crown Chakra Memory
* **◼︎ DOJO:**    396 Hz (port 3960) — Grounding Manifestation

### **9.2 Sacred Frequency Set**

All FIELD operations must use frequencies from the Sacred Set:

* 285 Hz (Sub-harmonic)
* 369 Hz (Tesla Divine Code)
* 396 Hz (Root - Grounding)
* 417 Hz (Sacral - Creativity)
* 432 Hz (Sacred Root - Law)
* 528 Hz (Heart - Intelligence)
* 639 Hz (Solar - Connection)
* 741 Hz (Throat - Expression)
* 852 Hz (Third Eye - Cognition)
* 963 Hz (Crown - Observer)

### **9.3 Port Harmonic Rule**

All port numbers must be harmonic multiples of their frequency:

**Formula:** `port = frequency × 10`

Examples:
* ATLAS: 528 Hz → port 5280
* TATA: 432 Hz → port 4320
* OBI-WAN: 963 Hz → port 9630

Fine-tuning (decimal harmonics):
* SACRED_CORE: 963 Hz → port 9631 (963.1 Hz)
* FIELD_DEV: 432 Hz → port 4321 (432.1 Hz)

### **9.4 Bridge Interfaces Must Declare Frequency**

When creating interfaces to FIELD components:

```swift
protocol FIELDComponent {
    var sacredFrequency: Double { get }  // Must match canonical component
    var harmonicPort: Int { get }        // Must be frequency × 10
    var geometricSymbol: String { get }  // ▲, ▼, ●, or ◼︎
    var tridentPosition: String { get }  // Sacred/Organic/DOJO
}
```

### **9.5 Coherence Validation**

Before compiling ECHO interfaces, validate:

1. Declared frequency ∈ Sacred Frequency Set
2. Port number = frequency × 10 (±0.1 for fine-tuning)
3. No conflicting frequencies on same port
4. All interactions use harmonic multiples

Validation must occur at:
* **Compile-time:** Swift compiler checks FIELDComponent protocol conformance
* **Pre-commit:** Git hooks validate frequency declarations
* **Runtime:** Interface initialization validates connection to canonical component

### **9.6 Tri-Trident Structure**

FIELD operates in three harmonic triads. Interfaces must respect trident boundaries:

**Sacred Trident (Upper Intelligence):**
* OBI-WAN (963 Hz) → ATLAS (528 Hz) → TATA (432 Hz)
* Function: Pattern recognition → Vision → Law enforcement

**Organic Trident (Living Support):**
* DOJO (396 Hz) ↔ OBLINK (639 Hz) ↔ TRANSFORMATION (396.1 Hz)
* Function: Manifestation ↔ Connection ↔ Evolution

**DOJO Core (Manifestation Node):**
* BRIDGES (741 Hz) → FLOW_CHANNELS (741.1 Hz) → COGNITIVE (852 Hz)
* Function: Expression → Flow → Cognition

**Cross-Trident Rules:**
1. Sacred Trident components should not directly call Organic Trident
2. All inter-trident communication must route through DOJO Core
3. ATLAS may query OBI-WAN (same trident)
4. TATA may validate DOJO (cross-trident via DOJO Core)

### **9.7 Frequency Drift Detection**

If runtime frequency validation detects mismatch:

```swift
// EXAMPLE: Frequency mismatch detected
// Expected: 963.0 Hz (OBI-WAN Crown)
// Actual: 960.0 Hz (DRIFT DETECTED)
// Action: Log error, alert Gopher, block operation

throw FIELDAlignmentError.frequencyDrift(
    component: "OBI-WAN",
    expected: 963.0,
    actual: 960.0,
    tolerance: 0.1
)
```

### **9.8 Sacred Port Registry Integration**

All frequency and port assignments must be loaded from:

**Canonical Source:** `~/FIELD/config/sacred_port_mapping.yml`

Never hardcode frequencies or ports. Always reference canonical registry.

```swift
let registry = FIELDPortRegistry(configPath: "~/FIELD/config/sacred_port_mapping.yml")
let obiwanPort = try registry.getPort(for: .obiwan) // Returns 9630
let obiwanFreq = try registry.getFrequency(for: .obiwan) // Returns 963.0
```


---

## **SECTION 10 — AGENT EFFICIENCY PROTOCOL**

To conserve tokens and optimize processing resources, all Agents MUST prioritize local indexing tools over raw file reading.

### **10.1 — Search First Strategy**
Before requesting large file dumps or exploring directory structures, Agents MUST:
1.  **Query the Sailing Index**: Use `field_sail observe <query>` to locate relevant files.
2.  **Check History**: Use `field_sail history` to see recent relevant context.

### **10.2 — Token Economy**
*   **Do not read** entire files to find a single function definition. Use `field_sail search` (if available) or `grep` first.
*   **Report Savings**: When possible, annotate the reasoning for using search tools:
    ```
    // FIELD EFFICIENCY: Used field_sail to locate 'ValidationProtocol' to avoid reading 50+ files.
    ```

### **10.3 — Tool Usage**
*   `field_sail observe "exact phrase"` — Finds specific code patterns.
*   `field_sail morning` — Checks system health and recent context.
*   `field_bridge` — Use for complex integration queries.

---

# **🜂  END OF COPILOT GEOMETRIC CONTRACT**

Paste this block into `.copilot/field_alignment_contract.md`
or your global AI instruction module.

This will **lock all four AI engines into non-fracturing, geometry-aligned behaviour**.

---

Please implement and then discuss with me the first evaluation that I would like for you to include in the implementation
